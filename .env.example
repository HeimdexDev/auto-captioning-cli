# Copy to .env and fill in. .env is gitignored and never committed.
# The same values live in the prototype's .env.
ANTHROPIC_API_KEY=
# Scoped read-only key (replit-caption-viewer) — used by resolve-media for 7-day presigns
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-northeast-2
